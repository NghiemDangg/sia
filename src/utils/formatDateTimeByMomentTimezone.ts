// import type { MomentInput } from 'moment'

// import moment from 'moment'

// const formatDateTimeByMomentTimezone = (inp?: MomentInput, format?: string, timezone?: string): string => {
//   return (
//     timezone && /^(\d+)|([\+-]\d{1,2}:\d{1,2})$/gm.test(timezone.trim())
//       ? moment(inp).utc().utcOffset(timezone)
//       : moment(inp)
//   ).format(format)
// }

// export default formatDateTimeByMomentTimezone
