export enum TypeApi {
  GET_PRICE,
  CONFIRM_CHANGE,
}

export enum StepPage {
  Banner = 1,
  AboutUs,
  BlogPage,
  Product,
  SignUp,
  Footer,
}

export enum OTPCodeTypeString {
  REGISTER = 'register',
  FORGOT_PASSWORD = 'forgot-password',
}

export enum OTPCodeType {
  REGISTER,
  FORGOT_PASSWORD,
}
